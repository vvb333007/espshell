// Minimal sketch - standalone shell. 
// Yes, as simple as that :)

#include <Arduino.h>

// Initialize Serial interface and let ESPShell do its job.
void setup() { Serial.begin(115200); }
void loop() { delay(9999); }
